# FastAPI + LangGraph RAG & Smart Query System
# NOTE: requires Python >= 3.11 (not a pip requirement — do not add it here)

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.18

# LLM / LangChain
langgraph>=0.2.50
langchain-core>=1.2.5
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-ollama>=0.2.0
# Local embedding (EMBEDDING_PROVIDER=huggingface). Pulls torch (~2GB).
# Skip if you only use cloud embedding APIs.
sentence-transformers>=3.0.0

# Database
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.30.0
aiomysql>=0.2.0
alembic>=1.14.0

# Vector Store
qdrant-client>=1.12.0
# chromadb>=0.5.0  # optional, for dev

# Cache
redis[hiredis]

# Auth
python-jose[cryptography]>=3.4.0
passlib[bcrypt]>=1.7.4
# passlib 1.7.x reads bcrypt.__about__, which bcrypt 4.1+ removed → pin to 4.0.1
# to silence "error reading bcrypt version" and keep hashing stable.
bcrypt==4.0.1

# Validation
pydantic>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.0.0

# Document Parsing
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.0
beautifulsoup4>=4.12.0
html5lib>=1.1
lxml>=5.3.0
markdown>=3.7.0

# Text Processing
langchain-text-splitters>=0.3.9

# SQL
sqlglot>=26.0.0

# Utils
httpx>=0.27.0
python-dotenv>=1.0.0
structlog>=24.4.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
httpx  # for test client